part of 'app_dimensions.dart';

class _Padding {
  const _Padding();

  double get defaultValue => 16.0;

  EdgeInsets defaultHorizontal() => EdgeInsets.symmetric(
        horizontal: defaultValue,
      );
}
